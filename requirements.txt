# Beijing AQI forecasting — full dependency list.
# Install with:    uv pip install -r requirements.txt

# --- Core numerics & I/O -------------------------------------------------
numpy>=1.26
pandas>=2.1
pyarrow>=15.0          # parquet read/write
requests>=2.31         # UCI dataset download

# --- Classical ML --------------------------------------------------------
scikit-learn>=1.4      # RandomForest, metrics
xgboost>=2.0
lightgbm>=4.3
joblib>=1.3            # model persistence

# --- Deep learning -------------------------------------------------------
torch>=2.2             # LSTM forecaster

# --- Plotting ------------------------------------------------------------
matplotlib>=3.8        # per-horizon MAE plots, confusion matrices

# --- Domain helpers ------------------------------------------------------
chinese-calendar>=1.10 # holiday flags for Beijing

# --- Testing -------------------------------------------------------------
pytest>=8.0
